# ============================================================
# requirements.txt — Daftar dependency Python
# ============================================================
# File ini digunakan oleh pip (package manager Python) untuk
# menginstall semua library yang dibutuhkan.
# Format: nama_package==versi (pin versi untuk reproducibility)
# ============================================================

# ── pika ────────────────────────────────────────────────────
# Client library untuk berkomunikasi dengan RabbitMQ dari Python.
# Pika mengimplementasikan protokol AMQP 0-9-1 (Advanced Message
# Queuing Protocol) yang merupakan protokol standar RabbitMQ.
# Kita gunakan untuk: connect, consume message, publish result.
pika==1.3.2

# ── markitdown ──────────────────────────────────────────────
# Library dari Microsoft untuk mengkonversi berbagai format
# dokumen (PDF, DOCX, PPTX, XLSX, HTML, CSV, dll.) menjadi
# Markdown. Ini adalah inti dari service converter kita.
markitdown[all]==0.1.1

# ── python-dotenv ──────────────────────────────────────────
# Library untuk membaca file .env dan memasukkan isinya ke
# environment variables (os.environ). Berguna untuk konfigurasi
# lokal tanpa Docker (saat development).
python-dotenv==1.1.1
